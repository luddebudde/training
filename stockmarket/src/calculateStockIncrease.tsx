// import { distance, graphBox, highestPoint, points, Vector } from "./main";

// export const calculateStockIncrease = (stock) => {
//   //   console.log(points);

//   const point: Vector = {
//     x: points[points.length - 1].x + distance,
//     y: points[points.length - 1].y * (Math.random() + 0.6),
//   };

//   if (point.y < highestPoint.y) {
//     // console.log("hihge");
//     highestPoint.y = point.y;
//   }

//   if (point.y > graphBox.height) {
//     // console.log("graphf");
//     point.y = graphBox.height - 50;
//   }

//   stock.currentValue = Math.round((graphBox.height - point.y) / 100);

//   // points.push(point);
//   return point;
// };
