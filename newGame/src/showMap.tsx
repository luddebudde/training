export const showMap = () => {
    const mapDiv = document.getElementById('mapDiv');
    if (mapDiv) {
      mapDiv.style.display = 'flex'; // Ändra display-egenskapen till 'flex' för att visa elementet
    }
  }