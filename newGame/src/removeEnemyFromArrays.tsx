import { bosses, enemies, Enemy, entities } from "./enemies/enemyTypes";

export const removeEnemyFromAllArrays = (enemy: Enemy) => {
  //   const arrays = [enemies, bosses, entities];
  //     arrays.forEach((arr) => {
  //       for (let i = arr.length - 1; i >= 0; i--) {
  //         if (arr[i].id === enemy.id) {
  //           arr.splice(i, 1);
  //         }
  //       }
  //     });
};
